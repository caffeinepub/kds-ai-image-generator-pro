import ToolStudio from '../../components/tools/ToolStudio';

export default function WeddingPreweddingEditorPage() {
  return <ToolStudio toolId="wedding-prewedding-editor" toolName="Wedding & Prewedding Editor" inputMode="edit" />;
}
