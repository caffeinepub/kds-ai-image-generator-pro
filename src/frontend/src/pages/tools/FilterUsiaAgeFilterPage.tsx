import ToolStudio from '../../components/tools/ToolStudio';

export default function FilterUsiaAgeFilterPage() {
  return <ToolStudio toolId="filter-usia-age-filter" toolName="Filter Usia (Age Filter)" inputMode="edit" />;
}
