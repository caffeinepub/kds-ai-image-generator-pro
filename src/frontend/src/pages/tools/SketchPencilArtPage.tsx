import ToolStudio from '../../components/tools/ToolStudio';

export default function SketchPencilArtPage() {
  return <ToolStudio toolId="sketch-pencil-art" toolName="Sketch & Pencil Art" inputMode="edit" />;
}
