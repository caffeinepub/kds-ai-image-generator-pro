import ToolStudio from '../../components/tools/ToolStudio';

export default function AgeTransformationFilterPage() {
  return <ToolStudio toolId="age-transformation-filter" toolName="Age Transformation Filter" inputMode="edit" />;
}
